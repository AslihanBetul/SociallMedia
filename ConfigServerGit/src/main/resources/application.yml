server:
  port: 8888

spring:
  application:
    name: config-server-git
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/AslihanBetul/socialmediaconfigserver.git"
          default-label: "main"
          skip-ssl-validation: true
          username: "AslihanBetul"
          password: ${Social-Media-Token}
